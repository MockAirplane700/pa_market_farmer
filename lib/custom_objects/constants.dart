import 'package:flutter/material.dart';

// The color of the application text
const Color primaryTextBackgroundColor = Colors.black;
// The color of the application app bar and it's elements
const Color primaryAppBarColor = Colors.white;
const Color primaryThemeDataColor = Colors.black;
// The color of the application's background
const Color primaryApplicationBackgroundColor = Colors.white;
